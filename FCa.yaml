likelihood:
  FCa:
    external: import_module('likelihoods_BAO_CMB_cov').FCa # Change the model here
    

params: # DONE 
  om:
    prior: {min: 0.1, max: 0.7}
    ref: 0.4
    proposal: 0.1
    latex: \Omega_{m}
  q:
    prior: {min: 0, max: 3}
    ref: 0.9
    proposal: 0.1
    latex: q
  n:
    prior: {min: -3, max: 2}
    ref: 0.01
    proposal: 0.1
    latex: n


#prior: # Addition prios to be imposed
#  x_eq_y_band: 'lambda x,y: stats.norm.logpdf(
#                   x - y, loc=0, scale=0.3)'

sampler: # DONE - CAN BE REFINED
  mcmc:
    max_tries: 10000

#theory: # Specifies the theory code(s) with which to compute the observables used by the likelihoods and their options

output: chains/CMB+BAO/FCa_CMB_BAO_high_n_prop