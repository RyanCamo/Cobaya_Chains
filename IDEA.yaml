likelihood:
  IDEA:
    external: import_module('likelihoods_SN_interp').IDEA # Change the model here
    

params: # DONE 
  cdm:
    prior: {min: 0.1, max: 0.8}
    ref: 0.3
    proposal: 0.001
    latex: \Omega_{cdm}
  w:
    prior: {min: -2, max: 2}
    ref: -0.9
    proposal: 0.01
    latex: \omega
  e:
    prior: {min: -0.5, max: 0.5}
    ref: 0.05
    proposal: 0.001
    latex: \varepsilon

#prior: # Addition prios to be imposed
#  x_eq_y_band: 'lambda x,y: stats.norm.logpdf(
#                   x - y, loc=0, scale=0.3)'

sampler: # DONE - CAN BE REFINED
  mcmc:
    max_tries: 10000

#theory: # Specifies the theory code(s) with which to compute the observables used by the likelihoods and their options

output: chains/SN_TESTS/IDEA_21