likelihood:
  wCDM:
    external: import_module('DES5YR_UNBIN_likelihoods').wCDM # Change the model here
    

params: # DONE 
  om:
    prior: {min: 0.2, max: 2}
    ref: 0.3
    proposal: 0.001
    latex: \Omega_m
  ol:
    prior: {min: 0.2, max: 2}
    ref: 0.75
    proposal: 0.001
    latex: \Omega_{\Lambda}
  w:
    prior: {min: -2, max: 2}
    ref: 0.5
    proposal: 0.001
    latex: \omega

#prior: # Addition prios to be imposed
#  x_eq_y_band: 'lambda x,y: stats.norm.logpdf(
#                   x - y, loc=0, scale=0.3)'

sampler: # DONE - CAN BE REFINED
  mcmc:
    max_tries: 10000

#theory: # Specifies the theory code(s) with which to compute the observables used by the likelihoods and their options

output: chains/wCDM_DES5YR_UNBIN # DONE